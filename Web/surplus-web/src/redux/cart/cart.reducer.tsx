const INITAL_STATE = {
    hidden: true
};

const cartReducer = (state = INITAL_STATE, action) => {
    switch(action.type) {
        default:
            return state;
    }
};

export default cartReducer;